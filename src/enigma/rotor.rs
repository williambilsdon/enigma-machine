use super::AlphabeticChar;

struct Rotor(Vec<char>);

impl Rotor {
    fn new() -> Self {
        Rotor(vec!['a', 'b', 'c'])
    }

    pub fn map_char(input: AlphabeticChar) -> AlphabeticChar {

    }
